import { Action } from '@ngrx/store';
import { IGamesState } from './../types/games-state/games-state.interface';

export function gamesReducer(state: IGamesState, action: Action) {
  return state;
}
